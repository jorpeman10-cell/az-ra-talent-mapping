# 外部顾问评估模块 P1 实施计划（服务化）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 P0 的问卷+评估引擎服务化进 headhunt-svc——候选人建档/自评链接（48h token）/HR 面评/定级定薪评估全流程 API + 自评页/模板编辑页 + `/data/candidates/` 存档，本地全链路测试通过。

**Architecture:** 新增 `headhunt-service/candidate/` 子包（P0 的 step6/step7 拷入改名，纯标准库）；新增 `candidate/store.py` 管存档与 token；API 端点追加在 `api/main.py`；两个静态页放 `web/`；HR 维护配置放 `config/`（volume 持久化）。部署（Task 7）单独门禁，等 Steven 确认才执行。

**Tech Stack:** FastAPI + pydantic（服务现有）、pytest + TestClient（新增 httpx 依赖）、vanilla JS 静态页（与 web/index.html 同风格）。

## Global Constraints

- 工作仓库：`C:\Users\EDY\Documents\kimi\workspace`，**新分支 `feature/external-candidate-p1`**（从 main 拉出）
- 所有代码在 `headhunt-service/` 内；`headhunt_model/` 的 P0 产物不动（唯一例外：Task 4 的空样本护栏同步回写，跑全量测试确认不破）
- 跑测试用 `py -3`（`python` 是坏 stub）；测试从 `headhunt-service/` 目录执行：`py -3 -m pytest tests/ -q`
- 引擎确定性红线不变：candidate/ 子包禁止 LLM、禁止网络调用、禁止读生产连接
- 金额单位：业绩/带宽年回款为万，月薪为元
- token：48h 有效、提交即失效、单次提交；`/q/{token}` 是唯一公开面（无鉴权，靠 token 不可猜）
- API 无鉴权（内网服务，与现有 `/api/config` POST 同口径）；外部公网暴露（nginx 路由）不在本计划内，Task 7 只留说明
- data/ 不进 git（薪酬合规，.gitignore 已有）；`config/candidate_grade_map.json`、`config/market_anchors.json` 初始为空表
- 现有端点与看板行为零变更：`tests/test_golden.py`（board 黄金回归）必须保持绿
- 部署只走 `deploy_headhunt.py`（upload → 镜像重建 → 双容器重建，含 lobe-network 并入）；**Task 7 执行前必须 Read lobehub TASKBOARD.md 并在 §5 登记，且需 Steven 明确放行**
- 存档 JSON 一律 `ensure_ascii=False, indent=1`（与现有输出层一致）

---

### Task 1: candidate/ 子包导入 + 服务级黄金夹具

**Files:**
- Create: `headhunt-service/candidate/__init__.py`
- Create: `headhunt-service/candidate/questionnaire.py`（拷自 headhunt_model/step6_questionnaire.py，头部注释标注来源）
- Create: `headhunt-service/candidate/engine.py`（拷自 headhunt_model/step7_candidate.py，仅改 import 行）
- Create: `headhunt-service/tests/fixtures/inputs/`（6 个 bundle JSON，拷自 headhunt_model/fixtures/inputs/）
- Create: `headhunt-service/tests/fixtures/expected/`（由脚本冻结生成）
- Create: `headhunt-service/tests/test_candidate_golden.py`
- Test: 即上述测试文件

**Interfaces:**
- Produces（后续任务消费，均为 P0 已验证的签名，原样可用）:
  - `candidate.questionnaire`: `default_template()`, `validate_template(t)`, `next_version(t)`, `score_questionnaire(t, responses, role)`, `verify_coefficient(hr_result)`, `VERIFY_COEF`
  - `candidate.engine`: `assess(bundle)`, `level_candidate(self_res, hr_res)`, `salary_band(grade, internal_samples, anchors)`, `load_internal_samples(salary_map, grade_map)`, `GRADE_TARGETS`, `GRADE_INDEX`, `line_match(tags)`, `tier(billing_wan)`, `breakeven_billing(monthly)`, `quick_npv(...)`

- [ ] **Step 1: 建分支并拷贝引擎文件**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace" && git checkout -b feature/external-candidate-p1
mkdir -p headhunt-service/candidate headhunt-service/tests/fixtures/inputs
cp headhunt_model/step6_questionnaire.py headhunt-service/candidate/questionnaire.py
cp headhunt_model/step7_candidate.py headhunt-service/candidate/engine.py
cp headhunt_model/fixtures/inputs/*.json headhunt-service/tests/fixtures/inputs/
touch headhunt-service/candidate/__init__.py
```

- [ ] **Step 2: 修改两处适配**

`candidate/questionnaire.py` 头部（第 2-4 行 docstring 后）不动，文件原样可用。
`candidate/engine.py`：把头部 import 区的 `from step6_questionnaire import VERIFY_COEF` 改为：

```python
from .questionnaire import VERIFY_COEF
```

两个文件头部各加一行来源注释（英文）：

```python
# Synced from headhunt_model/step6_questionnaire.py @ P0 (main c4cf532). Service-side fixes go here; port back to keep both green.
```

（engine.py 用对应文件名。）

- [ ] **Step 3: 写服务级黄金测试**

```python
# tests/test_candidate_golden.py
# Golden regression for the candidate engine (service copy), same fixtures as P0.
import json, os, sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from candidate.engine import assess

BASE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
FIX = os.path.join(BASE, "tests", "fixtures")


def test_candidate_golden():
    inputs_dir = os.path.join(FIX, "inputs")
    expected_dir = os.path.join(FIX, "expected")
    os.makedirs(expected_dir, exist_ok=True)
    for fname in sorted(os.listdir(inputs_dir)):
        if not fname.endswith(".json"):
            continue
        with open(os.path.join(inputs_dir, fname), encoding="utf-8") as f:
            bundle = json.load(f)
        actual = assess(bundle)
        exp_path = os.path.join(expected_dir, fname)
        if not os.path.exists(exp_path):
            with open(exp_path, "w", encoding="utf-8") as f:
                json.dump(actual, f, ensure_ascii=False, indent=1)
        with open(exp_path, encoding="utf-8") as f:
            expected = json.load(f)
        assert actual == expected, f"golden mismatch: {fname}"


if __name__ == "__main__":
    test_candidate_golden()
    print("candidate golden PASS")
```

- [ ] **Step 4: 运行并冻结**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace/headhunt-service" && py -3 -m pytest tests/test_candidate_golden.py -q && py -3 -m pytest tests/ -q
```

Expected: candidate golden PASS（首次运行写入 expected）；`test_golden.py`（board 回归）也绿（candidate/ 不影响 pipeline）。
**抽查**：`tests/fixtures/expected/case1_high_claim_low_verify.json` 的 `leveling.grade` 必须是 `SC1`（与 P0 冻结值一致，证明拷贝无损）。

- [ ] **Step 5: 提交**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace" && git add headhunt-service/candidate headhunt-service/tests/fixtures headhunt-service/tests/test_candidate_golden.py && git commit -m "feat(candidate): import P0 questionnaire+engine as candidate/ subpackage with service golden fixtures"
```

---

### Task 2: candidate/store.py 存档 + token + 状态机

**Files:**
- Create: `headhunt-service/candidate/store.py`
- Create: `headhunt-service/tests/conftest.py`（**全测试目录唯一的环境注入点**——pytest 采集阶段先于所有测试模块导入 conftest，避免多模块抢设环境变量导致 api.main 持有错误路径）
- Test: `headhunt-service/tests/test_candidate_store.py`

**Interfaces:**
- Produces:
  - 目录约定：存档根 = `os.environ.get("HEADHUNT_DATA_DIR") or {BASE}/data`（测试可注入临时目录）；每个候选人 `{root}/candidates/{cid}/profile.json|self_assess.json|hr_assess.json|assessment_{ts}.json`
  - `new_candidate(name, target_line="", notes="") -> dict`（含 cid/token/token_expires_at/status）
  - `get(cid) -> dict|None`；`update(cid, **fields) -> dict`
  - `save_json(cid, fname, obj)`；`load_json(cid, fname) -> obj|None`
  - `list_candidates() -> [dict]`（摘要列表）
  - `validate_token(token, now=None) -> (cid|None, reason|None)`，reason ∈ not_found/expired/already_submitted
  - `derive_status(rec) -> "CREATED"|"SELF_DONE"|"HR_DONE"|"ASSESSED"`
  - `TOKEN_TTL_HOURS = 48`；`CONFIG_DIR = os.environ.get("HEADHUNT_CONFIG_DIR") or {BASE}/config`

- [ ] **Step 1: 建 conftest.py（唯一环境注入点）**

```python
# tests/conftest.py
# Single env-injection point for the whole test dir. pytest imports conftest
# BEFORE any test module, so api.main/store see these paths deterministically.
# No test module may set these env vars itself.
import os
import sys
import tempfile

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
TMP = tempfile.mkdtemp()
os.environ["HEADHUNT_DATA_DIR"] = TMP
os.environ["HEADHUNT_CONFIG_DIR"] = os.path.join(TMP, "config")
```

- [ ] **Step 2: 写失败测试**

```python
# tests/test_candidate_store.py
# Storage, token TTL/single-use, status machine. Paths come from tests/conftest.py.
import os, sys
from datetime import datetime, timedelta, timezone

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from candidate import store
from candidate.store import CANDIDATES_DIR


def test_new_candidate_and_get():
    rec = store.new_candidate("张三", target_line="肿瘤线", notes="内推")
    assert rec["name"] == "张三" and rec["status"] == "CREATED"
    assert len(rec["token"]) >= 24 and rec["cid"].startswith("c")
    got = store.get(rec["cid"])
    assert got["token"] == rec["token"]
    assert os.path.isfile(os.path.join(CANDIDATES_DIR, rec["cid"], "profile.json"))


def test_token_valid_then_used():
    rec = store.new_candidate("李四")
    cid, reason = store.validate_token(rec["token"])
    assert cid == rec["cid"] and reason is None
    store.update(rec["cid"], self_submitted_at="2026-09-06T00:00:00+00:00")
    cid2, reason2 = store.validate_token(rec["token"])
    assert cid2 is None and reason2 == "already_submitted"


def test_token_expiry():
    rec = store.new_candidate("王五")
    future = datetime.now(timezone.utc) + timedelta(hours=49)
    cid, reason = store.validate_token(rec["token"], now=future)
    assert cid is None and reason == "expired"
    cid2, _ = store.validate_token(rec["token"], now=datetime.now(timezone.utc))
    assert cid2 == rec["cid"]


def test_unknown_token():
    cid, reason = store.validate_token("no-such-token")
    assert cid is None and reason == "not_found"


def test_status_machine_and_save_load():
    rec = store.new_candidate("赵六")
    assert store.derive_status(rec) == "CREATED"
    store.update(rec["cid"], self_submitted_at="t1")
    assert store.derive_status(store.get(rec["cid"])) == "SELF_DONE"
    store.save_json(rec["cid"], "hr_assess.json", {"scored": {"x": 1}})
    assert store.load_json(rec["cid"], "hr_assess.json")["scored"] == {"x": 1}
    store.update(rec["cid"], hr_submitted_at="t2", assessed_at="t3")
    assert store.derive_status(store.get(rec["cid"])) == "ASSESSED"
    assert store.load_json(rec["cid"], "nope.json") is None


def test_list_candidates():
    a = store.new_candidate("甲"); store.new_candidate("乙")
    names = [c["name"] for c in store.list_candidates()]
    assert "甲" in names and "乙" in names


if __name__ == "__main__":
    for n, f in sorted(list(globals().items())):
        if n.startswith("test_"):
            f(); print(n, "PASS")
```

- [ ] **Step 3: 跑红**

Run: `cd "C:/Users/EDY/Documents/kimi/workspace/headhunt-service" && py -3 -m pytest tests/test_candidate_store.py -q`
Expected: collection error / ModuleNotFoundError（store 不存在）

- [ ] **Step 4: 实现 store.py**

```python
# -*- coding: utf-8 -*-
"""Candidate archive: per-candidate JSON dirs, 48h single-use tokens, status machine.
Pure stdlib; no LLM, no network, no production connections."""
import json
import os
import secrets
from datetime import datetime, timedelta, timezone

BASE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DATA_DIR = os.environ.get("HEADHUNT_DATA_DIR") or os.path.join(BASE, "data")
CONFIG_DIR = os.environ.get("HEADHUNT_CONFIG_DIR") or os.path.join(BASE, "config")
CANDIDATES_DIR = os.path.join(DATA_DIR, "candidates")
TOKEN_TTL_HOURS = 48


def _now():
    return datetime.now(timezone.utc)


def _cid():
    return "c" + _now().strftime("%Y%m%d%H%M%S") + secrets.token_hex(2)


def _write(cid, fname, obj):
    os.makedirs(os.path.join(CANDIDATES_DIR, cid), exist_ok=True)
    with open(os.path.join(CANDIDATES_DIR, cid, fname), "w", encoding="utf-8") as f:
        json.dump(obj, f, ensure_ascii=False, indent=1)


def new_candidate(name, target_line="", notes=""):
    cid = _cid()
    rec = {
        "cid": cid, "name": name, "target_line": target_line or "", "notes": notes or "",
        "created_at": _now().isoformat(),
        "token": secrets.token_urlsafe(24),
        "token_expires_at": (_now() + timedelta(hours=TOKEN_TTL_HOURS)).isoformat(),
        "self_submitted_at": None, "hr_submitted_at": None, "assessed_at": None,
        "status": "CREATED",
    }
    _write(cid, "profile.json", rec)
    return rec


def get(cid):
    p = os.path.join(CANDIDATES_DIR, cid, "profile.json")
    if not os.path.isfile(p):
        return None
    with open(p, encoding="utf-8") as f:
        return json.load(f)


def update(cid, **fields):
    rec = get(cid)
    if rec is None:
        return None
    rec.update(fields)
    _write(cid, "profile.json", rec)
    return rec


def save_json(cid, fname, obj):
    _write(cid, fname, obj)


def load_json(cid, fname):
    p = os.path.join(CANDIDATES_DIR, cid, fname)
    if not os.path.isfile(p):
        return None
    with open(p, encoding="utf-8") as f:
        return json.load(f)


def list_candidates():
    out = []
    if not os.path.isdir(CANDIDATES_DIR):
        return out
    for cid in sorted(os.listdir(CANDIDATES_DIR)):
        rec = get(cid)
        if rec:
            out.append({k: rec.get(k) for k in ("cid", "name", "status", "created_at",
                                                "self_submitted_at", "hr_submitted_at", "assessed_at")})
    return out


def validate_token(token, now=None):
    """Return (cid, None) or (None, reason in not_found/expired/already_submitted)."""
    now = now or _now()
    if not os.path.isdir(CANDIDATES_DIR):
        return None, "not_found"
    for cid in os.listdir(CANDIDATES_DIR):
        rec = get(cid)
        if rec and rec.get("token") == token:
            if rec.get("self_submitted_at"):
                return None, "already_submitted"
            if now > datetime.fromisoformat(rec["token_expires_at"]):
                return None, "expired"
            return cid, None
    return None, "not_found"


def derive_status(rec):
    if rec.get("assessed_at"):
        return "ASSESSED"
    if rec.get("hr_submitted_at"):
        return "HR_DONE"
    if rec.get("self_submitted_at"):
        return "SELF_DONE"
    return "CREATED"
```

- [ ] **Step 5: 跑绿 + 全量回归**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace/headhunt-service" && py -3 -m pytest tests/test_candidate_store.py -q && py -3 -m pytest tests/ -q
```

Expected: store 6 项 PASS；board golden + candidate golden 不受影响

- [ ] **Step 6: 提交**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace" && git add headhunt-service/candidate/store.py headhunt-service/tests/conftest.py headhunt-service/tests/test_candidate_store.py && git commit -m "feat(candidate): archive store with 48h single-use tokens and status machine"
```

---

### Task 3: API 端点——intake / 自评链接 / 提交 / 面评 / 列表详情

**Files:**
- Modify: `headhunt-service/api/main.py`（文件末追加 candidate 端点区块）
- Modify: `headhunt-service/requirements.txt`（加 `httpx>=0.27`，TestClient 依赖）
- Test: `headhunt-service/tests/test_candidate_api.py`

**Interfaces:**
- Consumes: Task 1 的 questionnaire、Task 2 的 store
- Produces（Task 4/6 及 P2 网关消费）:
  - `POST /api/candidate/intake` body `{"name","target_line?","notes?"}` → `{"cid","name","status","self_url","expires_at"}`
  - `GET /q/{token}` → 自评页 HTML（404 not_found / 410 expired|already_submitted）
  - `GET /api/q/{token}/template` → `{"candidate_name","expires_at","template"}`
  - `POST /api/q/{token}/submit` body `{"answers":{qid:answer}}` → `{"ok":true,"status":"SELF_DONE"}`
  - `POST /api/candidate/{cid}/hr-assess` body `{"answers","interviewer?"}` → `{"ok":true,"status":"HR_DONE"}`
  - `GET /api/candidates` → `{"candidates":[...]}`
  - `GET /api/candidate/{cid}` → 完整档案（profile + 已有问卷 + 最新评估摘要）
  - 模板读写（本任务实现存储函数，Task 5 实现编辑页）: `load_template()` / `save_template(t)`——存 `CONFIG_DIR/questionnaire_template.json`，缺省写 default_template()

- [ ] **Step 1: 写失败测试**

```python
# tests/test_candidate_api.py
# API flow tests via TestClient. Paths come from tests/conftest.py.
import os, sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from fastapi.testclient import TestClient
from api.main import app

client = TestClient(app)

SELF_ANSWERS = {
    "perf_amount": 90, "perf_max_deal": 25, "perf_desc": "某CDMO总监单",
    "dom_tags": ["肿瘤"], "dom_share": "肿瘤100%",
    "speed_jc": "1-2天", "speed_case": "当天出mapping",
    "stab_moves": "1次", "stab_reason": "平台倒闭",
    "comp_share": "按规则分单",
}
HR_ANSWERS = {
    "perf_verify": "有部分佐证，数字合理", "perf_probe": "基本自洽，个别含糊",
    "dom_depth": "领域知识扎实",
    "speed_probe": "路径可行",
    "stab_verify": "基本可信",
    "comp_redline": "拒绝，边界感一般",
}


def test_intake_and_template_flow():
    r = client.post("/api/candidate/intake", json={"name": "张三", "target_line": "肿瘤线"})
    assert r.status_code == 200
    body = r.json()
    assert body["status"] == "CREATED" and body["self_url"].startswith("/q/")
    t = client.get(f"/api{body['self_url']}/template")
    assert t.status_code == 200
    assert t.json()["candidate_name"] == "张三"
    assert "dimensions" in t.json()["template"]


def test_submit_self_then_token_dead():
    r = client.post("/api/candidate/intake", json={"name": "李四"})
    token = r.json()["self_url"].split("/q/")[1]
    s = client.post(f"/api/q/{token}/submit", json={"answers": SELF_ANSWERS})
    assert s.status_code == 200 and s.json()["status"] == "SELF_DONE"
    again = client.post(f"/api/q/{token}/submit", json={"answers": SELF_ANSWERS})
    assert again.status_code == 410          # single use
    t2 = client.get(f"/api/q/{token}/template")
    assert t2.status_code == 410


def test_bad_token_404():
    assert client.get("/api/q/nope").status_code == 404
    assert client.get("/api/q/nope/template").status_code == 404


def test_hr_assess_and_detail():
    r = client.post("/api/candidate/intake", json={"name": "王五"})
    token = r.json()["self_url"].split("/q/")[1]
    cid = r.json()["cid"]
    client.post(f"/api/q/{token}/submit", json={"answers": SELF_ANSWERS})
    h = client.post(f"/api/candidate/{cid}/hr-assess",
                    json={"answers": HR_ANSWERS, "interviewer": "Steven"})
    assert h.status_code == 200 and h.json()["status"] == "HR_DONE"
    d = client.get(f"/api/candidate/{cid}")
    assert d.status_code == 200
    rec = d.json()
    assert rec["profile"]["name"] == "王五"
    assert rec["self_assess"]["scored"]["role"] == "self"
    assert rec["hr_assess"]["scored"]["role"] == "hr"
    lst = client.get("/api/candidates").json()["candidates"]
    assert any(c["cid"] == cid for c in lst)


def test_hr_assess_missing_candidate():
    assert client.post("/api/candidate/c99999999/hr-assess",
                       json={"answers": HR_ANSWERS}).status_code == 404


def test_board_still_works():
    # existing endpoints untouched: index served
    assert client.get("/").status_code == 200


if __name__ == "__main__":
    for n, f in sorted(list(globals().items())):
        if n.startswith("test_"):
            f(); print(n, "PASS")
```

- [ ] **Step 2: 跑红**

Run: `cd "C:/Users/EDY/Documents/kimi/workspace/headhunt-service" && py -3 -m pytest tests/test_candidate_api.py -q`
Expected: 404 on POST /api/candidate/intake（路由不存在）；若 httpx 缺失则先装：`py -3 -m pip install httpx`，并把 `httpx>=0.27` 追加进 requirements.txt

- [ ] **Step 3: 实现——requirements 加依赖 + api/main.py 追加端点**

`requirements.txt` 末尾追加一行：`httpx>=0.27`（TestClient 运行时依赖）。

`api/main.py` 文件末尾追加（import 区顶部补 `from datetime import timezone` 与 pydantic 已有）：

```python
# ===================== 外部顾问评估 (M3, P1) =====================
from datetime import timezone

from candidate.store import (new_candidate, get as get_candidate,
                             update as update_candidate, list_candidates,
                             validate_token, save_json, load_json,
                             derive_status, CONFIG_DIR)
from candidate.questionnaire import default_template, validate_template, score_questionnaire

TEMPLATE_PATH = os.path.join(CONFIG_DIR, "questionnaire_template.json")


def load_template():
    """Current questionnaire template; seeds from default on first call."""
    if not os.path.isfile(TEMPLATE_PATH):
        t = default_template()
        os.makedirs(CONFIG_DIR, exist_ok=True)
        with open(TEMPLATE_PATH, "w", encoding="utf-8") as f:
            json.dump(t, f, ensure_ascii=False, indent=1)
        return t
    with open(TEMPLATE_PATH, encoding="utf-8") as f:
        return json.load(f)


def save_template(t):
    os.makedirs(CONFIG_DIR, exist_ok=True)
    with open(TEMPLATE_PATH, "w", encoding="utf-8") as f:
        json.dump(t, f, ensure_ascii=False, indent=1)


class IntakeIn(BaseModel):
    name: str
    target_line: str = ""
    notes: str = ""


class SubmitIn(BaseModel):
    answers: dict


class HrAssessIn(BaseModel):
    answers: dict
    interviewer: str = ""


def _token_http_error(reason):
    return HTTPException(410 if reason in ("expired", "already_submitted") else 404, reason)


@app.post("/api/candidate/intake")
def api_candidate_intake(body: IntakeIn):
    if not body.name.strip():
        raise HTTPException(422, "name required")
    rec = new_candidate(body.name.strip(), body.target_line, body.notes)
    return {"cid": rec["cid"], "name": rec["name"], "status": rec["status"],
            "self_url": f"/q/{rec['token']}", "expires_at": rec["token_expires_at"]}


@app.get("/q/{token}")
def q_page(token: str):
    cid, reason = validate_token(token)
    if not cid:
        raise _token_http_error(reason)
    return FileResponse(os.path.join(BASE, "web", "q.html"))


@app.get("/api/q/{token}/template")
def api_q_template(token: str):
    cid, reason = validate_token(token)
    if not cid:
        raise _token_http_error(reason)
    rec = get_candidate(cid)
    return {"candidate_name": rec["name"], "expires_at": rec["token_expires_at"],
            "template": load_template()}


@app.post("/api/q/{token}/submit")
def api_q_submit(token: str, body: SubmitIn):
    cid, reason = validate_token(token)
    if not cid:
        raise _token_http_error(reason)
    result = score_questionnaire(load_template(), body.answers, "self")
    save_json(cid, "self_assess.json", {"scored": result, "answers": body.answers})
    update_candidate(cid, self_submitted_at=datetime.now(timezone.utc).isoformat(),
                     status="SELF_DONE")
    return {"ok": True, "status": "SELF_DONE"}


@app.post("/api/candidate/{cid}/hr-assess")
def api_hr_assess(cid: str, body: HrAssessIn):
    if get_candidate(cid) is None:
        raise HTTPException(404, f"candidate {cid} not found")
    result = score_questionnaire(load_template(), body.answers, "hr")
    save_json(cid, "hr_assess.json",
              {"scored": result, "answers": body.answers, "interviewer": body.interviewer,
               "submitted_at": datetime.now(timezone.utc).isoformat()})
    update_candidate(cid, hr_submitted_at=datetime.now(timezone.utc).isoformat(),
                     status="HR_DONE")
    return {"ok": True, "status": derive_status(get_candidate(cid))}


@app.get("/api/candidates")
def api_candidates():
    return {"candidates": list_candidates()}


@app.get("/api/candidate/{cid}")
def api_candidate_detail(cid: str):
    rec = get_candidate(cid)
    if rec is None:
        raise HTTPException(404, f"candidate {cid} not found")
    rec["status"] = derive_status(rec)
    latest = None
    for fname in sorted(os.listdir(os.path.join(store_dir(cid))), reverse=True):
        if fname.startswith("assessment_") and fname.endswith(".json"):
            latest = load_json(cid, fname)
            break
    return {"profile": rec, "self_assess": load_json(cid, "self_assess.json"),
            "hr_assess": load_json(cid, "hr_assess.json"), "latest_assessment": latest}


def store_dir(cid):
    from candidate.store import CANDIDATES_DIR
    return os.path.join(CANDIDATES_DIR, cid)
```

注意：`api_candidate_detail` 里 `os.listdir` 目录不存在时要防崩——`store_dir(cid)` 不存在时 latest 为 None（`get_candidate` 已通过则目录必存在，profile.json 在即目录在，安全）。

- [ ] **Step 4: 跑绿 + 全量**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace/headhunt-service" && py -3 -m pytest tests/test_candidate_api.py -q && py -3 -m pytest tests/ -q
```

Expected: API 6 项 PASS + 既有全绿

- [ ] **Step 5: 提交**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace" && git add headhunt-service/api/main.py headhunt-service/requirements.txt headhunt-service/tests/test_candidate_api.py && git commit -m "feat(candidate): intake/token questionnaire/HR assess/list/detail API endpoints"
```

---

### Task 4: assess 端点 + 内部样本接线 + 空数据护栏（含回写 P0）

**Files:**
- Modify: `headhunt-service/candidate/engine.py`（salary_band 空样本护栏）
- Modify: `headhunt_model/step7_candidate.py`（同一处护栏回写，防漂移）
- Create: `headhunt-service/config/candidate_grade_map.json`、`headhunt-service/config/market_anchors.json`（空表，HR 维护）
- Modify: `headhunt-service/api/main.py`（追加 assess 端点）
- Test: `headhunt-service/tests/test_candidate_assess.py`、`headhunt_model/tests/test_step7_salary.py`（加空样本用例）

**Interfaces:**
- Consumes: `pipeline.loaders.load_salary_map(path, departed_names, salary_overrides) -> (m, skipped)`，m 为 `{name: {"base_monthly","salary_annual"}}`；Task 1 的 `engine.load_internal_samples(salary_map, grade_map)`、`engine.assess(bundle)`
- Produces: `POST /api/candidate/{cid}/assess` → 引擎完整输出 JSON；存档 `assessment_{ts}.json`；两问卷不齐 → 409；评估成功 → status ASSESSED

- [ ] **Step 1: 写失败测试（服务侧）**

```python
# tests/test_candidate_assess.py
# assess endpoint: guards, wiring of internal samples from tmp CSV + grade map.
# Env dirs come from tests/conftest.py; seeding is request-time (endpoint reads
# files at request time, not import time), so import order does not matter.
import os, sys, json

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from fastapi.testclient import TestClient

TMP = os.environ["HEADHUNT_DATA_DIR"]
CFG = os.environ["HEADHUNT_CONFIG_DIR"]
os.makedirs(CFG, exist_ok=True)
SALARY_CSV = os.path.join(TMP, "salary.csv")
with open(SALARY_CSV, "w", encoding="utf-8-sig", newline="") as f:
    f.write("表头行\n姓名,月工资,职位工资,津贴,五险一金\n")  # 2 header rows per format
    f.write(",张三,15000,0,0,4000\n,李四,17000,0,0,4500\n")
with open(os.path.join(CFG, "candidate_grade_map.json"), "w", encoding="utf-8") as f:
    json.dump({"_comment": "test", "mapping": {"张三": "C1", "李四": "C1"}}, f, ensure_ascii=False)
with open(os.path.join(CFG, "market_anchors.json"), "w", encoding="utf-8") as f:
    json.dump({"_comment": "test", "anchors": {}}, f, ensure_ascii=False)
os.environ["HEADHUNT_SALARY_CSV"] = SALARY_CSV  # endpoint reads env override first

from api.main import app
client = TestClient(app)

SELF = {"perf_amount": 90, "perf_max_deal": 25, "perf_desc": "x",
        "dom_tags": ["肿瘤"], "dom_share": "肿瘤100%", "speed_jc": "1-2天",
        "speed_case": "x", "stab_moves": "1次", "stab_reason": "x", "comp_share": "按规则分单"}
HR = {"perf_verify": "有部分佐证，数字合理", "perf_probe": "基本自洽，个别含糊",
      "dom_depth": "领域知识扎实", "speed_probe": "路径可行",
      "stab_verify": "基本可信", "comp_redline": "拒绝，边界感一般"}


def _full_candidate(name):
    r = client.post("/api/candidate/intake", json={"name": name}).json()
    client.post(f"/api/q/{r['self_url'].split('/q/')[1]}/submit", json={"answers": SELF})
    client.post(f"/api/candidate/{r['cid']}/hr-assess", json={"answers": HR})
    return r["cid"]


def test_assess_blocks_when_incomplete():
    r = client.post("/api/candidate/intake", json={"name": "甲"}).json()
    resp = client.post(f"/api/candidate/{r['cid']}/assess")
    assert resp.status_code == 409


def test_assess_happy_path():
    cid = _full_candidate("乙")
    resp = client.post(f"/api/candidate/{cid}/assess")
    assert resp.status_code == 200
    out = resp.json()
    # 90 * 0.9 = 81 -> SC1 (no modifiers: hr stab/speed 4, dom 4 + overlap 70 -> +0.5 -> int(5.5-1+... ) see P0 case
    assert out["leveling"]["effective_billing_wan"] == 81.0
    assert out["status"] == "OK"
    # internal samples from grade map: C1 = [15000, 17000] -> median p50 16000
    assert out["band"]["p50"] == 16000
    assert out["breakeven_wan"] > 0
    d = client.get(f"/api/candidate/{cid}").json()
    assert d["profile"]["status"] == "ASSESSED"
    assert d["latest_assessment"]["leveling"]["effective_billing_wan"] == 81.0


def test_assess_empty_internal_data_guard():
    # wipe grade map -> internal samples empty -> band no_internal_data, breakeven None, warning
    with open(os.path.join(CFG, "candidate_grade_map.json"), "w", encoding="utf-8") as f:
        json.dump({"_comment": "x", "mapping": {}}, f, ensure_ascii=False)
    cid = _full_candidate("丙")
    out = client.post(f"/api/candidate/{cid}/assess").json()
    assert out["band"]["source"] == "no_internal_data"
    assert out["breakeven_wan"] is None
    assert any("grade_map" in w for w in out["warnings"])


if __name__ == "__main__":
    for n, f in sorted(list(globals().items())):
        if n.startswith("test_"):
            f(); print(n, "PASS")
```

- [ ] **Step 2: 跑红**

Run: `cd "C:/Users/EDY/Documents/kimi/workspace/headhunt-service" && py -3 -m pytest tests/test_candidate_assess.py -q`
Expected: 404/405（端点不存在）+ `no_internal_data` 断言失败（护栏未实现）

- [ ] **Step 3: 引擎护栏（两处同步改）**

`headhunt-service/candidate/engine.py` 的 `salary_band`，在 neighbor/whole-pool 回退之后、`_pct(samples)` 之前插入：

```python
    if not samples:  # no internal salary data at all (empty grade map)
        return {"p25": None, "p50": None, "p75": None,
                "source": "no_internal_data", "confidence": "LOW"}
```

同一函数所在模块的 `assess()`，把 `monthly = float(band["p50"])` 及其后 breakeven/npv 计算改为：

```python
    monthly = band["p50"]
    if monthly is None:
        warnings.append("no internal salary samples — fill config/candidate_grade_map.json "
                        "before salary band is meaningful")
        return {"status": "OK", "leveling": leveling, "band": band,
                "breakeven_wan": None, "breakeven_monthly": None,
                "npv_quick_at_target": None, "line_match": lm, "divergence": div,
                "confidence": "LOW", "warnings": warnings}
    monthly = float(monthly)
```

（其余 OK 路径不动。）

**回写 P0**：把完全相同的两处改动应用到 `headhunt_model/step7_candidate.py`，并在 `headhunt_model/tests/test_step7_salary.py` 追加：

```python
def test_band_no_internal_data():
    band = salary_band("C1", {}, {})
    assert band["source"] == "no_internal_data"
    assert band["p50"] is None and band["confidence"] == "LOW"
```

- [ ] **Step 4: 配置文件 + assess 端点**

`headhunt-service/config/candidate_grade_map.json`：

```json
{
  "_comment": "HR-maintained advisor->grade mapping for external-candidate salary bands. name must match salary_detail.csv. Fill before P3 live run.",
  "mapping": {}
}
```

`headhunt-service/config/market_anchors.json`：

```json
{
  "_comment": "HR-maintained per-grade market monthly-salary anchors {grade:{p25,p50,p75}}. Fill before P3 live run.",
  "anchors": {}
}
```

（这两个文件进 git——空表无敏感数据；容器内 config/ 是 volume，生产值由 HR 在服务器侧维护。）

`api/main.py` 追加：

```python
from candidate import engine as cand_engine
from pipeline.loaders import load_salary_map


def _internal_samples():
    """salary CSV x grade map -> {grade: [monthly,...]} for salary_band()."""
    csv_path = os.environ.get("HEADHUNT_SALARY_CSV")
    if not csv_path:
        cfg = yaml.safe_load(open(CONFIG_PATH, encoding="utf-8"))
        csv_path = os.path.join(BASE, cfg["data"]["salary_csv"])
    if not os.path.isfile(csv_path):
        return {}
    hr_cfg = (yaml.safe_load(open(CONFIG_PATH, encoding="utf-8")) or {}).get("hr", {})
    salary_map, _skipped = load_salary_map(
        csv_path,
        departed_names=hr_cfg.get("departed_names", []),
        salary_overrides=hr_cfg.get("salary_overrides"))
    grade_map_path = os.path.join(CONFIG_DIR, "candidate_grade_map.json")
    grade_map = {}
    if os.path.isfile(grade_map_path):
        grade_map = json.load(open(grade_map_path, encoding="utf-8")).get("mapping", {})
    return cand_engine.load_internal_samples(salary_map, grade_map)


def _anchors():
    p = os.path.join(CONFIG_DIR, "market_anchors.json")
    if os.path.isfile(p):
        return json.load(open(p, encoding="utf-8")).get("anchors", {})
    return {}


@app.post("/api/candidate/{cid}/assess")
def api_candidate_assess(cid: str):
    if get_candidate(cid) is None:
        raise HTTPException(404, f"candidate {cid} not found")
    self_a = load_json(cid, "self_assess.json")
    hr_a = load_json(cid, "hr_assess.json")
    if not self_a or not hr_a:
        raise HTTPException(409, f"questionnaires incomplete: "
                                 f"self={'yes' if self_a else 'no'}, hr={'yes' if hr_a else 'no'}")
    out = cand_engine.assess({"self": self_a["scored"], "hr": hr_a["scored"],
                              "internal_samples": _internal_samples(), "anchors": _anchors()})
    ts = datetime.now().strftime("%Y%m%d_%H%M%S")
    save_json(cid, f"assessment_{ts}.json", out)
    update_candidate(cid, assessed_at=datetime.now(timezone.utc).isoformat(),
                     status="ASSESSED")
    return out
```

- [ ] **Step 5: 跑绿 + 两侧全量**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace/headhunt-service" && py -3 -m pytest tests/ -q
cd "C:/Users/EDY/Documents/kimi/workspace/headhunt_model" && py -3 -m pytest tests/ -q && py -3 make_golden.py
```

Expected: 服务侧全绿（含新 3 项）；headhunt_model 39 项全绿 + 黄金 6 ok（护栏不改变有样本路径的输出）

- [ ] **Step 6: 提交**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace" && git add headhunt-service/candidate/engine.py headhunt-service/api/main.py headhunt-service/config/candidate_grade_map.json headhunt-service/config/market_anchors.json headhunt-service/tests/test_candidate_assess.py headhunt_model/step7_candidate.py headhunt_model/tests/test_step7_salary.py && git commit -m "feat(candidate): assess endpoint wiring internal samples; empty-data guard ported to both engine copies"
```

---

### Task 5: 模板读写端点 + HR 编辑页

**Files:**
- Modify: `headhunt-service/api/main.py`（GET/PUT /api/template + 历史版本归档）
- Create: `headhunt-service/web/template.html`
- Test: `headhunt-service/tests/test_candidate_template.py`

**Interfaces:**
- Consumes: Task 3 的 `load_template()/save_template(t)`、Task 1 的 `validate_template/next_version`
- Produces:
  - `GET /api/template` → 当前模板
  - `PUT /api/template` body 完整模板 dict → 校验失败 422（错误列表）；成功 `{saved: true, version: N}`；服务端强制 `version = 旧版本+1` 并把旧版归档到 `{DATA_DIR}/templates/v{旧}.json`
  - `GET /template` → 编辑页 HTML

- [ ] **Step 1: 写失败测试**

```python
# tests/test_candidate_template.py
# Template GET/PUT with validation, forced version bump, history archive.
# Env dirs come from tests/conftest.py.
import os, sys, json

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from fastapi.testclient import TestClient
from api.main import app

client = TestClient(app)


def test_get_seeds_default():
    t = client.get("/api/template").json()
    assert t["template_id"] == "consultant_v1" and t["version"] == 1
    assert len(t["dimensions"]) == 5


def test_put_invalid_rejected():
    t = client.get("/api/template").json()
    t["dimensions"][0]["weight"] = 0.9     # breaks the 1.0 sum
    r = client.put("/api/template", json=t)
    assert r.status_code == 422
    assert any("weight" in e for e in r.json()["detail"])


def test_put_valid_bumps_version_and_archives():
    t = client.get("/api/template").json()
    t["dimensions"][0]["name"] = "业绩证据（修订）"
    r = client.put("/api/template", json=t)
    assert r.status_code == 200 and r.json()["version"] == 2
    t2 = client.get("/api/template").json()
    assert t2["version"] == 2 and t2["dimensions"][0]["name"] == "业绩证据（修订）"
    hist = os.path.join(TMP, "templates", "v1.json")
    assert os.path.isfile(hist)
    assert json.load(open(hist, encoding="utf-8"))["version"] == 1


def test_editor_page_served():
    assert client.get("/template").status_code == 200


if __name__ == "__main__":
    for n, f in sorted(list(globals().items())):
        if n.startswith("test_"):
            f(); print(n, "PASS")
```

- [ ] **Step 2: 跑红**

Run: `py -3 -m pytest tests/test_candidate_template.py -q`（从 headhunt-service/）
Expected: 405/404

- [ ] **Step 3: 实现端点**

`api/main.py` 追加：

```python
@app.get("/api/template")
def api_template_get():
    return load_template()


@app.get("/template")
def template_page():
    return FileResponse(os.path.join(BASE, "web", "template.html"))


@app.put("/api/template")
def api_template_put(t: dict):
    errors = validate_template(t)
    if errors:
        raise HTTPException(422, errors)
    old = load_template()
    t = dict(t)
    t["version"] = old.get("version", 1) + 1   # server-controlled, never trust client
    os.makedirs(os.path.join(DATA_DIR_TEMPLATES), exist_ok=True)
    with open(os.path.join(DATA_DIR_TEMPLATES, f"v{old.get('version', 1)}.json"),
              "w", encoding="utf-8") as f:
        json.dump(old, f, ensure_ascii=False, indent=1)
    save_template(t)
    return {"saved": True, "version": t["version"]}
```

并在 candidate 端点区块顶部补常量：`DATA_DIR_TEMPLATES = os.path.join(os.environ.get("HEADHUNT_DATA_DIR") or os.path.join(BASE, "data"), "templates")`（注意与 store.DATA_DIR 同源；import 区加 `from candidate.store import DATA_DIR as CAND_DATA_DIR` 后 `DATA_DIR_TEMPLATES = os.path.join(CAND_DATA_DIR, "templates")` 更稳，二选一取后者）。

- [ ] **Step 4: 编辑页 web/template.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>问卷模板编辑器 — 外部顾问评估</title>
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif;
         background: #f5f6f8; color: #1a1a2e; }
  header { background: #1a1a2e; color: #fff; padding: 18px 32px; display: flex;
           justify-content: space-between; align-items: baseline; }
  header h1 { font-size: 18px; font-weight: 600; }
  header .meta { font-size: 12px; opacity: .7; }
  .wrap { max-width: 900px; margin: 24px auto; padding: 0 20px; }
  .card { background: #fff; border-radius: 10px; padding: 16px 20px; margin-bottom: 16px;
          box-shadow: 0 1px 3px rgba(0,0,0,.06); }
  .dim-head { display: flex; gap: 12px; align-items: center; margin-bottom: 10px; }
  .dim-head input[name=dim-name] { flex: 1; font-size: 15px; font-weight: 600;
                                   border: none; border-bottom: 1px dashed #ccc; padding: 4px; }
  .w { width: 90px; }
  label { font-size: 13px; color: #555; }
  .q { border-left: 3px solid #e8e8ef; padding: 8px 12px; margin: 8px 0; }
  .q .label { width: 100%; border: 1px solid #e0e0e8; border-radius: 6px; padding: 6px 8px;
              font-size: 13px; margin-bottom: 6px; }
  .opts textarea { width: 100%; font-size: 12px; font-family: Consolas, monospace;
                   border: 1px solid #e0e0e8; border-radius: 6px; padding: 6px; min-height: 68px; }
  .hint { font-size: 12px; color: #999; margin-top: 4px; }
  #sum { font-weight: 700; }
  #sum.ok { color: #177245; } #sum.bad { color: #b03030; }
  button.primary { background: #1a1a2e; color: #fff; border: none; border-radius: 8px;
                   padding: 10px 28px; font-size: 14px; cursor: pointer; }
  button.primary:disabled { opacity: .4; cursor: not-allowed; }
  #msg { font-size: 13px; margin-left: 12px; }
</style>
</head>
<body>
<header><h1>问卷模板编辑器</h1><div class="meta">外部顾问评估 · 历史存档不受编辑影响</div></header>
<div class="wrap">
  <div class="card">
    权重合计：<span id="sum">-</span>
    <button class="primary" id="publish" style="float:right">发布新版本</button>
    <span id="msg"></span>
  </div>
  <div id="dims"></div>
</div>
<script>
let T = null;
const $ = s => document.querySelector(s);

// options textarea format: one per line, "label | score | redline(可选1)"
function optsToText(opts, type) {
  if (type !== "choice" && type !== "multi") return "";
  return (opts || []).map(o => [o.label, o.score, o.redline ? 1 : ""].filter(x => x !== "")
    .join(" | ")).join("\n");
}
function textToOpts(text) {
  return text.split("\n").map(l => l.trim()).filter(Boolean).map(l => {
    const parts = l.split("|").map(s => s.trim());
    return {label: parts[0], score: Number(parts[1] || 0),
            redline: parts[2] === "1"};
  });
}

function render() {
  $("#dims").innerHTML = "";
  T.dimensions.forEach((d, di) => {
    const card = document.createElement("div"); card.className = "card";
    let html = `<div class="dim-head">
      <input name="dim-name" value="${d.name}" data-di="${di}">
      <span><label>权重</label> <input class="w" type="number" step="0.05" min="0" max="1"
        value="${d.weight}" data-w="${di}"></span>
      <span class="hint">id: ${d.id}（不可改）</span></div>`;
    for (const role of ["self_questions", "hr_questions"]) {
      html += `<h4 style="margin:10px 0 4px">${role === "self_questions" ? "自评卷" : "面评卷"}</h4>`;
      d[role].forEach((q, qi) => {
        html += `<div class="q">
          <div style="font-size:11px;color:#999">${q.id} · ${q.type}</div>
          <input class="label" value="${(q.label || "").replace(/"/g, "&quot;")}"
                 data-role="${role}" data-di="${di}" data-qi="${qi}">
          ${q.type === "choice" || q.type === "multi" ?
            `<div class="opts"><textarea data-role="${role}" data-di="${di}" data-qi="${qi}"
              >${optsToText(q.options, q.type)}</textarea>
             <div class="hint">每行一个选项：文案 | 分值 | 1(红线,仅choice)</div></div>` : ""}`;
      });
    }
    card.innerHTML = html;
    $("#dims").appendChild(card);
  });
  $("#dims").querySelectorAll("[data-w]").forEach(el =>
    el.addEventListener("input", () => { T.dimensions[+el.dataset.di].weight = +el.value; sum(); }));
  $("#dims").querySelectorAll("[data-role][class=label]").forEach(el =>
    el.addEventListener("input", () => {
      T.dimensions[+el.dataset.di][el.dataset.role][+el.dataset.qi].label = el.value; }));
  $("#dims").querySelectorAll("textarea[data-role]").forEach(el =>
    el.addEventListener("input", () => {
      T.dimensions[+el.dataset.di][el.dataset.role][+el.dataset.qi].options =
        textToOpts(el.value); }));
  $("#dims").querySelectorAll("[name=dim-name]").forEach(el =>
    el.addEventListener("input", () => { T.dimensions[+el.dataset.di].name = el.value; }));
  sum();
}
function sum() {
  const s = T.dimensions.reduce((a, d) => a + (+d.weight || 0), 0);
  $("#sum").textContent = (s * 100).toFixed(0) + "%";
  $("#sum").className = Math.abs(s - 1) < 1e-6 ? "ok" : "bad";
  $("#publish").disabled = Math.abs(s - 1) > 1e-6;
}
$("#publish").onclick = async () => {
  $("#msg").textContent = "发布中…";
  const r = await fetch("/api/template", {method: "PUT",
    headers: {"Content-Type": "application/json"}, body: JSON.stringify(T)});
  if (r.ok) { const j = await r.json(); $("#msg").textContent = "已发布 v" + j.version; }
  else { const j = await r.json(); $("#msg").textContent = "被拒: " + JSON.stringify(j.detail); }
};
fetch("/api/template").then(r => r.json()).then(t => { T = t; render(); });
</script>
</body>
</html>
```

（多选题选项分值在计分中不使用，仅 choice 用分值与红线——与引擎规则一致。）

- [ ] **Step 5: 跑绿 + 全量**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace/headhunt-service" && py -3 -m pytest tests/test_candidate_template.py -q && py -3 -m pytest tests/ -q
```

Expected: template 4 项 PASS + 全量绿。浏览器抽查（可选）：`py -3 -m uvicorn api.main:app --port 18899` 后开 `http://127.0.0.1:18899/template` 改一个选项文案→发布→v2。

- [ ] **Step 6: 提交**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace" && git add headhunt-service/api/main.py headhunt-service/web/template.html headhunt-service/tests/test_candidate_template.py && git commit -m "feat(candidate): template GET/PUT with forced version bump, history archive, HR editor page"
```

---

### Task 6: 候选人自评页 + 全链路 e2e

**Files:**
- Create: `headhunt-service/web/q.html`
- Test: `headhunt-service/tests/test_candidate_e2e.py`

**Interfaces:**
- Consumes: Task 3 的 `/api/q/{token}/template` + `/api/q/{token}/submit`
- Produces: 完整可发候选人的自评页（PC/移动自适应）；`GET /q/{token}` 已在 Task 3 挂上

- [ ] **Step 1: 写 e2e 失败测试**

```python
# tests/test_candidate_e2e.py
# Full flow: intake -> template -> self submit -> hr assess -> assess -> archive files.
# Env dirs come from tests/conftest.py.
import os, sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from fastapi.testclient import TestClient
TMP = os.environ["HEADHUNT_DATA_DIR"]
from api.main import app

client = TestClient(app)

SELF = {"perf_amount": 90, "perf_max_deal": 25, "perf_desc": "某CDMO管线总监单，客户续约二期",
        "dom_tags": ["肿瘤", "糖尿病/CVRM"], "dom_share": "肿瘤70% 糖尿病30%",
        "speed_jc": "1-2天", "speed_case": "接到JC当天出mapping次日首推",
        "stab_moves": "1次", "stab_reason": "原平台团队解散", "comp_share": "按规则分单"}
HR = {"perf_verify": "有部分佐证，数字合理", "perf_probe": "基本自洽，个别含糊",
      "dom_depth": "领域知识扎实", "speed_probe": "路径可行",
      "stab_verify": "基本可信", "comp_redline": "拒绝，边界感一般"}


def test_e2e_full_flow():
    r = client.post("/api/candidate/intake",
                    json={"name": "e2e测试候选人", "target_line": "肿瘤线", "notes": "e2e"}).json()
    cid, token = r["cid"], r["self_url"].split("/q/")[1]

    page = client.get(f"/q/{token}")
    assert page.status_code == 200 and "问卷" in page.text

    tpl = client.get(f"/api/q/{token}/template").json()
    assert tpl["candidate_name"] == "e2e测试候选人"

    s = client.post(f"/api/q/{token}/submit", json={"answers": SELF})
    assert s.status_code == 200

    h = client.post(f"/api/candidate/{cid}/hr-assess",
                    json={"answers": HR, "interviewer": "e2e"})
    assert h.status_code == 200

    a = client.post(f"/api/candidate/{cid}/assess")
    assert a.status_code == 200
    out = a.json()
    assert out["status"] == "OK"
    assert out["leveling"]["verify_coefficient"] == 0.9
    assert out["line_match"]["top_line"] in ("肿瘤线", "糖尿病线")

    d = client.get(f"/api/candidate/{cid}").json()
    assert d["profile"]["status"] == "ASSESSED"
    assert d["self_assess"]["scored"]["template_version"] >= 1
    assert d["hr_assess"]["scored"]["role"] == "hr"
    # archive files on disk
    cdir = os.path.join(TMP, "candidates", cid)
    files = os.listdir(cdir)
    assert "profile.json" in files and "self_assess.json" in files and "hr_assess.json" in files
    assert any(f.startswith("assessment_") for f in files)


def test_e2e_redline_reject():
    r = client.post("/api/candidate/intake", json={"name": "红线测试"}).json()
    cid, token = r["cid"], r["self_url"].split("/q/")[1]
    client.post(f"/api/q/{token}/submit", json={"answers": SELF})
    client.post(f"/api/candidate/{cid}/hr-assess",
                json={"answers": dict(HR, comp_redline="承认做过类似行为")})
    out = client.post(f"/api/candidate/{cid}/assess").json()
    assert out["status"] == "REJECT_REVIEW"


if __name__ == "__main__":
    for n, f in sorted(list(globals().items())):
        if n.startswith("test_"):
            f(); print(n, "PASS")
```

- [ ] **Step 2: 跑红**

Run: `py -3 -m pytest tests/test_candidate_e2e.py -q`
Expected: 第一个断言失败（`/q/{token}` 返回的 q.html 不存在 → FileResponse 500）

- [ ] **Step 3: 实现自评页 web/q.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>顾问自评问卷</title>
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif;
         background: #f5f6f8; color: #1a1a2e; }
  .wrap { max-width: 640px; margin: 0 auto; padding: 24px 16px 80px; }
  h1 { font-size: 20px; margin-bottom: 4px; }
  .sub { font-size: 13px; color: #888; margin-bottom: 20px; }
  .card { background: #fff; border-radius: 10px; padding: 16px 18px; margin-bottom: 14px;
          box-shadow: 0 1px 3px rgba(0,0,0,.06); }
  .card h3 { font-size: 15px; margin-bottom: 10px; }
  .q { margin: 12px 0 16px; }
  .q .label { font-size: 14px; margin-bottom: 6px; }
  .opt { display: flex; align-items: center; gap: 8px; padding: 7px 10px; font-size: 14px;
         border: 1px solid #e8e8ef; border-radius: 8px; margin: 4px 0; cursor: pointer; }
  .opt input { transform: scale(1.15); }
  input[type=number], textarea { width: 100%; border: 1px solid #e0e0e8; border-radius: 8px;
         padding: 9px 10px; font-size: 14px; font-family: inherit; }
  textarea { min-height: 72px; }
  .bar { position: fixed; bottom: 0; left: 0; right: 0; background: #fff;
         border-top: 1px solid #eee; padding: 12px 16px; text-align: center; }
  button.primary { background: #1a1a2e; color: #fff; border: none; border-radius: 8px;
                   padding: 12px 48px; font-size: 15px; }
  .state { text-align: center; padding: 60px 20px; font-size: 15px; color: #666; }
  .err { color: #b03030; font-size: 13px; margin-top: 8px; }
</style>
</head>
<body>
<div class="wrap"><div id="app" class="state">加载中…</div></div>
<div class="bar" id="bar" style="display:none"><button class="primary" onclick="submit()">提交</button>
<div class="err" id="err"></div></div>
<script>
let T = null, NAME = "", answers = {};

async function boot() {
  const token = location.pathname.split("/q/")[1];
  const r = await fetch(`/api/q/${token}/template`);
  if (!r.ok) {
    const why = {"expired": "链接已过期（48小时有效），请联系 HR 重新发送",
                 "already_submitted": "问卷已提交，无需重复填写"}[r.json().detail] || "链接无效";
    document.getElementById("app").innerHTML = `<div class="state">${why}</div>`;
    return;
  }
  const j = await r.json();
  T = j.template; NAME = j.candidate_name;
  render();
}
function render() {
  let html = `<h1>顾问自评问卷</h1><div class="sub">${NAME} · 请如实填写，约 10 分钟</div>`;
  T.dimensions.forEach((d, di) => {
    html += `<div class="card"><h3>${d.name}</h3>`;
    d.self_questions.forEach((q, qi) => {
      html += `<div class="q"><div class="label">${q.label}</div>`;
      if (q.type === "choice")
        q.options.forEach((o, oi) => html +=
          `<label class="opt"><input type="radio" name="${q.id}" value="${o.label}"
            onchange="answers['${q.id}']='${o.label.replace(/'/g, "\\'")}'">${o.label}</label>`);
      else if (q.type === "multi")
        q.options.forEach(o => html +=
          `<label class="opt"><input type="checkbox" value="${o.label}"
            onchange="multi('${q.id}','${o.label.replace(/'/g, "\\'")}')">${o.label}</label>`);
      else if (q.type === "number")
        html += `<input type="number" oninput="answers['${q.id}']=+this.value">`;
      else
        html += `<textarea oninput="answers['${q.id}']=this.value"></textarea>`;
      html += `</div>`;
    });
    html += `</div>`;
  });
  document.getElementById("app").innerHTML = html;
  document.getElementById("bar").style.display = "";
}
function multi(qid, label) {
  const cur = answers[qid] || [];
  const i = cur.indexOf(label);
  if (i >= 0) cur.splice(i, 1); else cur.push(label);
  answers[qid] = cur;
}
async function submit() {
  const token = location.pathname.split("/q/")[1];
  document.getElementById("err").textContent = "";
  const r = await fetch(`/api/q/${token}/submit`, {method: "POST",
    headers: {"Content-Type": "application/json"}, body: JSON.stringify({answers})});
  if (r.ok) {
    document.getElementById("app").innerHTML =
      `<div class="state">✅ 已提交，感谢配合。后续请保持电话畅通。</div>`;
    document.getElementById("bar").style.display = "none";
  } else if (r.status === 422) {
    document.getElementById("err").textContent = "提交被拒：请检查必填项";
  } else {
    document.getElementById("err").textContent = "提交失败（" + r.status + "），请稍后重试";
  }
}
boot();
</script>
</body>
</html>
```

- [ ] **Step 4: 跑绿 + 全量**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace/headhunt-service" && py -3 -m pytest tests/test_candidate_e2e.py -q && py -3 -m pytest tests/ -q
```

Expected: e2e 2 项 PASS + 服务全量绿（board golden / candidate golden / store / api / template / assess 全部）

- [ ] **Step 5: 提交**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace" && git add headhunt-service/web/q.html headhunt-service/tests/test_candidate_e2e.py && git commit -m "feat(candidate): self-assessment page and full-flow e2e test"
```

---

### Task 7: 部署与收尾（⚠️ 门禁任务，需 Steven 放行 + TASKBOARD 登记）

**Files:**
- Modify: `headhunt-service/README.md`（新端点/页面/配置说明）
- Modify: `headhunt_model/外部顾问评估模块设计文档_v1.md`（P1 状态标注）

**Interfaces:**
- Produces: 生产可用的 candidate 端点 + 两个页面 + `/data/candidates/` 存档目录

- [ ] **Step 1: README 更新**

在 `headhunt-service/README.md` 追加一节（中文）：

```markdown
## 外部顾问评估（M3, P1）

- 端点：POST /api/candidate/intake · GET /api/q/{token}/template · POST /api/q/{token}/submit ·
  POST /api/candidate/{cid}/hr-assess · POST /api/candidate/{cid}/assess ·
  GET /api/candidates · GET /api/candidate/{cid} · GET/PUT /api/template
- 页面：/q/{token}（候选人自评，48h 单次 token）· /template（HR 编辑器，发布即版本+1 并归档旧版）
- 存档：/app/data/candidates/{cid}/（profile / self_assess / hr_assess / assessment_{ts}）
- HR 配置（服务器 config/ volume 内维护）：
  - questionnaire_template.json：当前问卷模板（首访自动生成默认版）
  - candidate_grade_map.json：顾问→职级映射（定薪带宽的内部样本来源，P3 前必须填）
  - market_anchors.json：分级市场底薪锚点（一期手录，二期分位库替换）
- 测试：py -3 -m pytest tests/ -q（candidate golden 与 board golden 同套跑）
```

- [ ] **Step 2: 设计文档标注**

`外部顾问评估模块设计文档_v1.md` §八 P1 行后追加：
`> P1 已于 2026-09-XX 完成（本地全链路 e2e + N 项测试全绿，commit 见 git log）；生产部署与公网 /q/ 路由见 README 与部署记录。`

- [ ] **Step 3: 部署前检查清单（逐项确认后才可继续）**

1. **Read `C:\Users\EDY\.kimi\lobehub\TASKBOARD.md`**——确认无其他 Agent 占用 headhunt-service / 部署锁（§3 占用表、§1 部署锁）
2. 在 TASKBOARD §5 追加一行变更日志（P1 端点/页面/存档上线，commit hash）
3. 向 Steven 确认放行（本步是硬门禁，未放行则到此为止，把分支保留）

- [ ] **Step 4: 部署（放行后执行）**

```bash
cd "C:/Users/EDY/Documents/kimi/workspace/headhunt-service" && py -3 deploy_headhunt.py all
```

Expected: 镜像重建成功、双容器 Up、`curl http://127.0.0.1:18801/api/board` 返回看板、
`curl -s -X POST http://127.0.0.1:18801/api/candidate/intake -H 'Content-Type: application/json' -d '{"name":"部署冒烟"}'` 返回 cid 与 self_url。

- [ ] **Step 5: 部署后验证 + 收尾提交**

```bash
py -3 deploy_headhunt.py verify
```

Expected: board 正常 + 无其他容器异常重启。
公网说明（不在本次执行）：候选人自评链接要外发，需在宿主 nginx 加 `location /q/ { proxy_pass http://127.0.0.1:18801; }`（含 /api/q/ 前缀路由），与 hiijob.cn 现有证书/域名规划一起定，留给部署日与 Steven 现场确认。

```bash
cd "C:/Users/EDY/Documents/kimi/workspace" && git add headhunt-service/README.md "headhunt_model/外部顾问评估模块设计文档_v1.md" && git commit -m "docs: P1 endpoints/pages/config docs; mark P1 complete"
```

---

## P1 完成后的下一步（不在本计划内）

1. **P2 计划**：lobehub worktree 网关侧——4 个 CANDIDATE capability packs + 结果卡 + AI 叙事护栏（数字溯源校验）
2. **公网 /q/ 路由**：nginx location + 域名规划（部署日与 Steven 确认）
3. **HR 侧**：填服务器 `config/candidate_grade_map.json`（10 名在职顾问职级）与 `market_anchors.json`
4. P0 终审遗留的 12 项 Minor 分诊清单见 `.superpowers/sdd/progress.md`
